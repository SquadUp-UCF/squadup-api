node_modules/
dist/
# Jest's HTML/lcov report — `npm run test:cov` writes it here (rootDir is src,
# coverageDirectory is ../coverage), regenerated on every run.
coverage/
.env
*.log
.DS_Store
# Local database dumps (may contain real PII — never commit)
prod-dump/
*.bson
*.dump
.git 2
# Uploaded user files (profile pictures, etc.) — not tracked in git
/uploads/

# Scratch dir for banner candidate review (npm run banners:fetch -- --candidates)
/public/sports/_candidates/
